import React from 'react';

const Login = (props) => {
  return (
    <div>
      <span>Login</span>
      <input autoComplete="off" type="text" onChange={} name="username" placeholder="username" />
      <input autoComplete="off" type="text" onChange={} name="password" placeholder="password" />
      <button type="button">Submit</button>
    </div>
  );
};
export default Login;
